export default class Password {
    constructor(password, question, answer, userPassword) {
        this.password = password;
        this.question = question;
        this.answer = answer;
        this.userPassword = userPassword;
    }
}